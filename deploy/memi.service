[Unit]
Description=Memi
After=network.target

[Service]
User=memi
WorkingDirectory=/opt/memi
ExecStart=/opt/memi/.venv/bin/gunicorn memi.app:app --bind 127.0.0.1:8080 --workers 2
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
